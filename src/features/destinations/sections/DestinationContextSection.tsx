import { AnalysisButton } from "../../../components/ui/buttons";
import type { DestinationLandingContent } from "../destination-content";

export function DestinationContextSection({ destination, onStartPlanning }: { destination: DestinationLandingContent; onStartPlanning: () => void }) {
  const { context } = destination;
  return <section className="ep-us-destination-context"><div className="ep-container"><div className="ep-us-destination-context__heading"><p className="ep-us-kicker">Seu destino, com contexto</p><h2>{context.title} <em>{context.titleHighlight}</em></h2><p>{context.intro} <strong>{context.introHighlight}</strong></p></div><div className="ep-us-destination-context__grid">{context.cards.map(({ place, label, image, imageAlt, copy, route, tone }) => <article key={place} className={`ep-us-destination-context__card is-${tone}`}><div className="ep-us-destination-context__visual"><img src={image} alt={imageAlt} loading="lazy" /><span>{place}</span></div><div className="ep-us-destination-context__copy"><small>{label}</small><h3>{place}</h3><p>{copy}</p><strong>{route}</strong></div></article>)}</div><div className="ep-us-destination-context__closing"><p>Seu destino não precisa estar nesta lista. <strong>A viagem do seu pet é analisada da origem até onde a sua família vai chegar.</strong></p><AnalysisButton onClick={onStartPlanning}>Quero planejar a viagem do meu pet</AnalysisButton></div></div></section>;
}
